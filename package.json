{
  "name": "sslcommerz-sdk",
  "private": true,
  "scripts": {
    "install": "python -m pip install -r requirements.txt",
    "test": "echo no test defined",
    "build": "python setup.py sdist bdist_wheel",
    "deploy": "twine upload --repository testpypi dist/*",
    "deploy:prod": "twine upload dist/*"
  }
}
